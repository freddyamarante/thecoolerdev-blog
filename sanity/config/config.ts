type Config = {
  projectId: string
  dataset: string
  apiVersion: string
}

const config: Config = {
  projectId: '7eiqcloi',
  dataset: 'production',
  apiVersion: '2023-05-06',
}

export default config
